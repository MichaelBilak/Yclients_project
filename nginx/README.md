# Nginx

Production routing details are intentionally not documented in this public
repository.

Keep real domains, upstreams, certificate paths, exposed routes and access
control decisions in private operational documentation. Checked-in nginx files
must stay generic and must not include production hosts, IP addresses or
business-specific routing notes.
