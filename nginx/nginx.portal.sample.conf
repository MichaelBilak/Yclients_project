# Sample: product host (app) + API + separate Metabase host (mb).
# Copy fragments into conf.d or merge with default.conf after replacing domains.

# --- app.your-domain.com: customer portal + API ---
# server {
#     listen 443 ssl;
#     server_name app.your-domain.com;
#     ssl_certificate     /etc/nginx/ssl/live/app.your-domain.com/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/live/app.your-domain.com/privkey.pem;
#
#     location /api/ {
#         proxy_pass http://api:8000/;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     location / {
#         root /var/www/portal;
#         try_files $uri $uri/ /index.html;
#     }
# }

# --- mb.your-domain.com: Metabase (analysts only) ---
# server {
#     listen 443 ssl;
#     server_name mb.your-domain.com;
#     ssl_certificate     /etc/nginx/ssl/live/mb.your-domain.com/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/live/mb.your-domain.com/privkey.pem;
#
#     location / {
#         proxy_pass http://metabase:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
