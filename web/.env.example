# API base URL (no trailing slash), e.g. https://api.example.com or http://127.0.0.1:8000
VITE_API_BASE=http://127.0.0.1:8000
# Optional: only for local demos — do not ship a production secret in a public SPA
VITE_API_KEY=
